{
  "name": "tsoa-typedi",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "start": "npm run build && node dist/server.js",
    "build": "rm -rf dist && tsoa spec-and-routes && tsc",
    "postman": "openapi2postmanv2 -s src/swagger/swagger.json -o src/swagger/postman.json -p"
  },
  "author": "Tharakesh Pulikonda",
  "license": "ISC",
  "description": "tsoa, typedi, express, typescript",
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.2",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "openapi-to-postmanv2": "^5.3.3",
    "reflect-metadata": "^0.2.2",
    "swagger-ui-express": "^5.0.1",
    "tsoa": "^6.6.0",
    "typedi": "^0.10.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.2",
    "@types/swagger-ui-express": "^4.1.8",
    "typescript": "^5.9.3"
  }
}
